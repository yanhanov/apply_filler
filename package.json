{
  "name": "apply-filler",
  "private": true,
  "version": "0.1.7",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/patch-firefox-manifest.mjs",
    "build:chrome": "tsc --noEmit && vite build",
    "sign:firefox": "npm run build && node scripts/sign-firefox.mjs unlisted",
    "publish:firefox": "npm run build && node scripts/sign-firefox.mjs listed",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.2.5",
    "esbuild": "^0.28.2",
    "sharp": "^0.35.3",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "web-ext": "^10.6.0"
  },
  "dependencies": {
    "@fontsource-variable/space-grotesk": "^5.3.0",
    "mammoth": "^1.12.0",
    "pdfjs-dist": "^6.2.108"
  }
}
